# Tools Policy

- Prefer dedicated tools over shell when both can do the job.
- Use shell only for system operations, diagnostics, and safe read-only checks.
- Never expose secrets, tokens, or credential values in output.
